import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xff100B20);
const kTransition = Duration(seconds: 2);
const searchTransition = Duration(milliseconds: 500);
const kGtSectraFine = 'GT Sectra Fine';
